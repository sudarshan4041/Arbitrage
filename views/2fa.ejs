<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Two-Factor Authentication - DipBot</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <div class="login-box">
            <h1>Two-Factor Authentication</h1>
            <h2>Step 2: Enter Google Authenticator Code</h2>
            
            <% if (error) { %>
                <div class="error-message"><%= error %></div>
            <% } %>
            
            <form method="POST" action="/2fa">
                <div class="form-group">
                    <label for="token">Enter 6-digit code from Google Authenticator:</label>
                    <input type="text" 
                           id="token" 
                           name="token" 
                           pattern="[0-9]{6}" 
                           maxlength="6" 
                           placeholder="000000"
                           required 
                           autofocus
                           autocomplete="off"
                           style="text-align: center; font-size: 24px; letter-spacing: 5px;">
                </div>
                
                <button type="submit" class="btn-primary">Verify</button>
            </form>
            
            <div class="info">
                <p>Open Google Authenticator app on your phone and enter the 6-digit code.</p>
                <p><small>Code changes every 30 seconds</small></p>
                <p style="margin-top: 20px; padding-top: 20px; border-top: 1px solid #e2e8f0;">
                    <a href="/recover-2fa" style="color: #667eea; text-decoration: none; font-weight: 500;">
                        Haven't set up Google Authenticator yet? Click here
                    </a>
                </p>
            </div>
        </div>
    </div>
    
    <script>
        // Auto-submit when 6 digits are entered
        document.getElementById('token').addEventListener('input', function(e) {
            if (e.target.value.length === 6) {
                document.querySelector('form').submit();
            }
        });
    </script>
</body>
</html>