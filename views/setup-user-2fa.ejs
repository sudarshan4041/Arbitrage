<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Setup 2FA - STINGFU</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: linear-gradient(135deg, #0f0f0f 0%, #1a1a1a 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #ffffff;
        }
        
        .container {
            width: 100%;
            max-width: 520px;
            padding: 20px;
        }
        
        .setup-box {
            background: rgba(30, 35, 45, 0.95);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            padding: 40px;
            backdrop-filter: blur(10px);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
        }
        
        h1 {
            font-size: 32px;
            font-weight: 600;
            margin-bottom: 10px;
            text-align: center;
            color: #ffffff;
        }
        
        .subtitle {
            color: #8a8a8a;
            text-align: center;
            margin-bottom: 30px;
            font-size: 16px;
        }
        
        .qr-container {
            background: white;
            padding: 20px;
            border-radius: 12px;
            margin: 25px 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        
        .secret-display {
            background: rgba(45, 50, 60, 0.8);
            border: 1px solid rgba(255, 255, 255, 0.15);
            border-radius: 10px;
            padding: 15px;
            margin: 20px 0;
            word-break: break-all;
            font-family: 'Courier New', monospace;
            font-size: 12px;
            color: #4a9eff;
            text-align: center;
        }
        
        .instructions {
            background: rgba(45, 50, 60, 0.5);
            border-left: 3px solid #4a9eff;
            padding: 15px;
            margin: 25px 0;
            border-radius: 5px;
        }
        
        .instructions h3 {
            color: #4a9eff;
            margin-bottom: 10px;
            font-size: 18px;
        }
        
        .instructions ol {
            margin-left: 20px;
            color: #b0b0b0;
            line-height: 1.8;
        }
        
        .form-group {
            margin: 25px 0;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #e0e0e0;
            font-size: 14px;
            font-weight: 500;
        }
        
        .input-wrapper {
            position: relative;
            display: flex;
            align-items: center;
        }
        
        input[type="text"] {
            width: 100%;
            padding: 14px 16px;
            background: rgba(45, 50, 60, 0.8);
            border: 1px solid rgba(255, 255, 255, 0.15);
            border-radius: 10px;
            color: #ffffff;
            font-size: 18px;
            letter-spacing: 5px;
            text-align: center;
            transition: all 0.3s ease;
        }
        
        input[type="text"]::placeholder {
            color: #6a6a6a;
            letter-spacing: normal;
        }
        
        input[type="text"]:focus {
            outline: none;
            border-color: #4a9eff;
            background: rgba(45, 50, 60, 1);
            box-shadow: 0 0 0 3px rgba(74, 158, 255, 0.1);
        }
        
        .btn-verify {
            width: 100%;
            padding: 14px;
            background: linear-gradient(135deg, #4a9eff 0%, #3b82f6 100%);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            letter-spacing: 0.5px;
        }
        
        .btn-verify:hover {
            background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(74, 158, 255, 0.3);
        }
        
        .error-message {
            background: rgba(220, 53, 69, 0.15);
            border: 1px solid rgba(220, 53, 69, 0.3);
            color: #ff6b6b;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
            font-size: 14px;
        }
        
        .success-message {
            background: rgba(40, 167, 69, 0.15);
            border: 1px solid rgba(40, 167, 69, 0.3);
            color: #4ade80;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
            font-size: 14px;
        }
        
        .email-display {
            text-align: center;
            color: #4a9eff;
            margin-bottom: 20px;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="setup-box">
            <h1>Setup Two-Factor Authentication</h1>
            <p class="subtitle">Secure your account with Google Authenticator</p>
            
            <div class="email-display">Account: <%= email %></div>
            
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="error-message"><%= message %></div>
            <% } else if (message && !error) { %>
                <div class="success-message"><%= message %></div>
            <% } %>
            
            <div class="instructions">
                <h3>What is Two-Factor Authentication (2FA)?</h3>
                <p style="color: #b0b0b0; margin-bottom: 15px; line-height: 1.6;">
                    2FA adds an extra layer of security to your account. Even if someone knows your password, 
                    they can't access your account without your phone. Google Authenticator generates a new 
                    6-digit code every 30 seconds that you'll use to login.
                </p>
                
                <h3>Setup Instructions:</h3>
                <ol>
                    <li><strong>Download Google Authenticator</strong>
                        <ul style="list-style: none; margin: 5px 0;">
                            <li>📱 iPhone: Search "Google Authenticator" in App Store</li>
                            <li>🤖 Android: Search "Google Authenticator" in Play Store</li>
                        </ul>
                    </li>
                    <li><strong>Open the app and tap the + button</strong></li>
                    <li><strong>Choose "Scan QR Code"</strong> and point your camera at the code below</li>
                    <li><strong>You'll see "DipBot"</strong> added to your app with a 6-digit code</li>
                    <li><strong>Enter that code below</strong> to complete setup</li>
                </ol>
                
                <div style="background: rgba(255, 193, 7, 0.1); border: 1px solid rgba(255, 193, 7, 0.3); padding: 12px; border-radius: 5px; margin-top: 15px;">
                    <p style="color: #ffc107; margin: 0; font-size: 13px;">
                        ⚠️ <strong>Important:</strong> Save the manual entry key below in a safe place. 
                        You'll need it if you lose your phone or need to set up a new device.
                    </p>
                </div>
            </div>
            
            <div class="qr-container">
                <img src="<%= qrCode %>" alt="QR Code for Google Authenticator">
            </div>
            
            <div class="secret-display">
                <strong>Manual Entry Key:</strong><br>
                <%= secret %>
            </div>
            
            <form method="POST" action="/verify-user-2fa">
                <div class="form-group">
                    <label for="token">Enter 6-digit verification code:</label>
                    <div class="input-wrapper">
                        <input type="text" 
                               id="token" 
                               name="token" 
                               placeholder="000000" 
                               maxlength="6" 
                               pattern="[0-9]{6}" 
                               required 
                               autofocus>
                    </div>
                </div>
                
                <button type="submit" class="btn-verify">Verify and Complete Setup</button>
            </form>
        </div>
    </div>
    
    <script>
        // Auto-focus and format input
        document.getElementById('token').addEventListener('input', function(e) {
            this.value = this.value.replace(/[^0-9]/g, '');
        });
    </script>
</body>
</html>